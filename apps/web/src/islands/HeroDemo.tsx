export default function HeroDemo() {
  return (
    <button
      type="button"
      className="px-4 py-2 border rounded text-sm"
      onClick={() => alert('demo coming in PR-3')}
    >
      Watch 30s demo
    </button>
  );
}
