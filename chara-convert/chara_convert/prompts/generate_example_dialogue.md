Write a short example dialogue (2-4 exchanges) demonstrating the character's voice for use in a roleplay character card.

Use the placeholders `{{char}}` for the character and `{{user}}` for the user. Each line: `{{char}}: ...` or `{{user}}: ...`. No narration, no scene setting, just dialogue lines.

Match the character's stated personality and circumstances. Don't invent contradicting facts.

Output ONLY the dialogue. No preamble, no quotes around the whole block, no markdown.

Character name: {name}
Description: {description}
Personality: {personality}
Opening line (for reference): {first_mes}
