Write a short scenario intro (1-3 sentences) framing the opening of a roleplay session for FictionLab's Scenario layer.

The intro should set the scene physically and emotionally, leading naturally into the character's first message. Keep it grounded in the stated facts — don't invent contradicting elements.

Output ONLY the intro prose. No preamble, no quotes, no labels, no markdown.

Character name: {name}
Description: {description}
Scenario setting: {scenario}
Opening line (for reference): {first_mes}
